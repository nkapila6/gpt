[project]
name = "gpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "marimo>=0.22.0",
    "numpy>=2.2.6",
    "python-dotenv>=1.2.2",
    "torch>=2.11.0",
]
